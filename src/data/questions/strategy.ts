import type { Question } from '../types'

// No published strategy answers currently. Kept as an empty array (rather than
// removing the archetype) so it's a one-line drop-in if a strategy question is
// authored later; the archetype disappears from nav automatically while empty.

export const strategyQuestions: Question[] = []
